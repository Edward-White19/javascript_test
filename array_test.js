// Purpose: Experimentation with JavaScript (Arrays)
// Author:  Edward White
// Date:    19/08/24

/*
** You can put anything in an array.
*/

// An array containing a Number, a String, and a Boolean.
let array = [4, "string", true];

// A FUNCTION that prints the array elements individually.
function printArrayElements(array) {
    for (let i = 0; i < array.length; i++) {
    }
}

printArrayElements(array);
